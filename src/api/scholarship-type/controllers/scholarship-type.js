'use strict';

/**
 * scholarship-type controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::scholarship-type.scholarship-type');
